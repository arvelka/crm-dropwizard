package pl.net.divo.crm;

import junit.framework.TestCase;

import org.junit.Test;


public class LoginTest extends TestCase {
	/*@ClassRule
	public static final DropwizardAppRule<CrmConfiguration> RULE =
					new DropwizardAppRule<CrmConfiguration>(CrmApplication.class, ResourceHelpers.resourceFilePath("/projects/crm/app.yaml"));
*/
	@Test
	public void testLoginHandlerRedirectsAfterPost() {/*
		Client client = new JerseyClientBuilder(RULE.getEnvironment()).build("test client");
		String test = client.resource(
			String.format("http://localhost:%d/user", RULE.getLocalPort())
		).get(String.class);

		System.out.println(test);*/
		
	}
}
